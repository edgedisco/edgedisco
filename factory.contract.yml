# Rust is the active port. Factory cards must not run the legacy Python suite
# unless their card explicitly names a Python compatibility or packaging gate.
language: rust
test_command: cargo fmt --all --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --workspace
